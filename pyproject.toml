[project]
name = "weathermcp"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["mcp>=1.27", "requests>=2.32"]

[project.scripts]
weathermcp = "weathermcp:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"